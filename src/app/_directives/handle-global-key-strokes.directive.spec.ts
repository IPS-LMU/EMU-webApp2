import { HandleGlobalKeyStrokesDirective } from './handle-global-key-strokes.directive';

describe('HandleGlobalKeyStrokesDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleGlobalKeyStrokesDirective();
    expect(directive).toBeTruthy();
  });
});
